name: search_algorithms_dart
description:  A Dart package providing various search algorithms and a quick sort utility for sorting lists of strings. Includes implementations of linear search, binary search, and jump search.
version: 1.0.1
repository: https://github.com/Isfandiyor-Dev/search_algorithms.git

environment:
  sdk: ^3.4.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
